version: "2"

services:
  mongo:
    container_name: mongo
    image: mongo:latest
    volumes:
      - ~/data/arbitrage/mongo:/data/db
  redis:
    container_name: redis
    image: redis:latest
    volumes:
      - ~/data/arbitrage/redis:/data
  discord:
    build: .
    container_name: arbitrage
    ports:
      - 3000:3000
    depends_on:
      - mongo
      - redis
